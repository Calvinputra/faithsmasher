<p class="sidebar-section-label">Session</p>
<p class="sidebar-session-name">{{ session.name }}</p>

{% include 'components/ui/sidebar-link.twig' with {
    href: '/dashboard/' ~ session.id,
    icon: 'ri-layout-grid-line',
    label: 'Overview',
    active: active|default('') == 'overview'
} %}

<p class="sidebar-section-label">Setup</p>
{% include 'components/ui/sidebar-link.twig' with {
    href: '/sessions/' ~ session.id ~ '/participants',
    icon: 'ri-group-line',
    label: 'Peserta Session',
    active: active|default('') == 'participants'
} %}
{% include 'components/ui/sidebar-link.twig' with {
    href: '/sessions/' ~ session.id ~ '/rules',
    icon: 'ri-file-list-3-line',
    label: 'Game Rules',
    active: active|default('') == 'rules'
} %}
{% include 'components/ui/sidebar-link.twig' with {
    href: '/sessions/' ~ session.id ~ '/edit',
    icon: 'ri-edit-line',
    label: 'Edit Session',
    active: active|default('') == 'edit'
} %}

<p class="sidebar-section-label">Match</p>
{% include 'components/ui/sidebar-link.twig' with {
    href: '/sessions/' ~ session.id ~ '/matches',
    icon: 'ri-trophy-line',
    label: 'Match Hub',
    active: active|default('') == 'matches'
} %}
{% include 'components/ui/sidebar-link.twig' with {
    href: '/sessions/' ~ session.id ~ '/matches/manual',
    icon: 'ri-drag-move-2-line',
    label: 'Manual Setup',
    active: active|default('') == 'manual'
} %}
{% include 'components/ui/sidebar-link.twig' with {
    href: '/sessions/' ~ session.id ~ '/matches/preview',
    icon: 'ri-eye-line',
    label: 'Preview Bracket',
    active: active|default('') == 'preview'
} %}
{% include 'components/ui/sidebar-link.twig' with {
    href: '/sessions/' ~ session.id ~ '/matches/counter',
    icon: 'ri-bar-chart-grouped-line',
    label: 'Player Counter',
    active: active|default('') == 'counter'
} %}
