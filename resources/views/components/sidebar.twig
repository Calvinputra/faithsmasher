<p class="sidebar-section-label">Session</p>
<p class="sidebar-session-name">{{ session.name }}</p>

{% include 'components/ui/sidebar-link.twig' with {
    href: '/dashboard/' ~ session.id,
    icon: 'ri-layout-grid-line',
    label: 'Overview',
    active: active|default('') == 'overview'
} %}

<p class="sidebar-section-label">Setup</p>
{% include 'components/ui/sidebar-link.twig' with {
    href: '/sessions/' ~ session.id ~ '/participants',
    icon: 'ri-group-line',
    label: 'Peserta Session',
    active: active|default('') == 'participants'
} %}


<p class="sidebar-section-label">Match</p>
{% include 'components/ui/sidebar-link.twig' with {
    href: '/sessions/' ~ session.id ~ '/matches',
    icon: 'ri-trophy-line',
    label: 'Match Hub',
    active: active|default('') == 'matches'
} %}

