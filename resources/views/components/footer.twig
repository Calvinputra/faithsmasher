<footer class="mt-auto border-t border-border bg-surface">
    <div class="mx-auto flex max-w-6xl flex-col gap-2 px-4 py-8 text-sm text-navy-500 sm:flex-row sm:items-center sm:justify-between sm:px-6">
        <p>&copy; {{ "now"|date("Y") }} {{ app.name }}</p>
        <p>Turnamen Badminton · Bagan & Pencatatan Skor</p>
    </div>
</footer>
